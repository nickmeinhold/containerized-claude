# OS artifacts
.DS_Store

# Secrets — never commit these
.env
msmtprc
.claude-credentials.json

# Personal Claude config (baked into image at build time)
claude-config/

# Runtime artifacts
workspace/
agent-logs/

# Session capture artifacts
playwright-storage.json
node_modules/
